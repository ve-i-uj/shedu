input{
    file{
        path => "/opt/kbengine/assets/logs/machine.log"
        start_position => beginning
    }
}
output {
    elasticsearch {
      hosts => ["http://kbe-log-elk-elastic:9200"]
      index => "kbe-logs-%{+YYYY.MM.dd}"
  }
}
